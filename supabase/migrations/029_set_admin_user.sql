-- ============================================================
-- Migration 029: назначаем admin владельцу системы
-- ============================================================
-- Применяется отдельно от 028, потому что Postgres не разрешает
-- использовать новое значение enum в той же транзакции, где оно
-- было добавлено (ALTER TYPE ADD VALUE).
--
-- Этот файл нужно запускать ПОСЛЕ 028.
-- ============================================================

UPDATE profiles
   SET role = 'admin'
 WHERE id IN (SELECT id FROM auth.users WHERE email = 'wsupkz@gmail.com');
